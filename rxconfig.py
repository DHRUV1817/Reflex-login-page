import reflex as rx

config = rx.Config(
    app_name="login_demo",                       # must match the folder name
    db_url="sqlite:///reflex.db",               # Use local SQLite for testing
)
